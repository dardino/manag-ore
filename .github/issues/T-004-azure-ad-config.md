Title: T-004 — Configurazione Azure AD per API e SPA
Labels: security, task
Milestone: M4

Descrizione

Configurare placeholders per Azure AD in `appsettings` e le istruzioni per registrare l'API e la SPA su Azure AD. Aggiungere i sample di configurazione per MSAL in frontend.

Deliverables

- appsettings.Development.json con sezione `AzureAd` (placeholders)
- README.md dettagli per registrazione AD e scope `access_as_user`
- Test d'integrazione per validare token (mocks ok per il primo step)
