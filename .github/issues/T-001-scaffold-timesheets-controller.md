Title: T-001 — Scaffold backend api/v1 con controller Timesheets
Labels: backend, task
Milestone: M1

Descrizione

Creare il controller `TimesheetsController` con endpoints CRUD base per `TimeEntry` (Get list, Get by id, Post, Put, Delete) e il relativo modello `TimeEntry`.

Files da creare/aggiornare

- `src/Api/ManagOre.Api/Controllers/TimesheetsController.cs`
- `src/Api/ManagOre.Api/Models/TimeEntry.cs`

Note

Questa task serve come starting point per le API degli orari. Aggiungere test unitari per il controller.
